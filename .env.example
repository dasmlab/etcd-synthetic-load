# Copy to .env (gitignored). NEVER commit real credentials.
# Rotate any password that was shared in chat/history.

OC_SERVER=https://api.2026-prod-2.ocp.dasmlab.org:6443
OC_USER=dasm
OC_PASSWORD=change-me

# Optional: mount/use kubeconfig instead of user/pass
# KUBECONFIG=/data/kubeconfig

# Data directory inside container (mount host ./data here)
ESL_DATA_DIR=/data
ESL_RUNTIME_CONFIG=/data/runtime.yaml
